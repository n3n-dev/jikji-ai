# 가이드 문서 작성 규칙

이 문서는 `content/docs` 아래 가이드 문서를 작성하거나 수정할 때 따르는 기준이다.

기준 문서:
- `content/docs/account-creation.mdx`
- `content/docs/container-creation/index.mdx`
- `content/docs/container-creation/container-access.mdx`

## 1. 기본 원칙

- 문서는 신규 사용자가 그대로 따라 할 수 있어야 한다.
- 실제 서비스 화면과 문구를 우선한다.
- 설명은 한국어로 쓰되, 사용자가 화면에서 직접 찾아야 하는 UI 라벨은 원문을 유지한다.
- 제목은 문서형으로 쓴다.
- 본문은 행동 중심으로 쓴다.
- 확인 단계는 "무엇을", "어디에서" 확인하는지 분명해야 한다.
- description은 실제 문서 범위를 넘기지 않는다.

## 2. 문서 구조

모든 절차형 문서는 아래 구조를 기본으로 한다.

1. frontmatter
2. 번호가 있는 단계별 섹션
3. 필요한 경우 Callout
4. 단계별 이미지

개요 페이지는 아래 구조를 기본으로 한다.

1. frontmatter
2. 문서 목적과 이용 순서를 설명하는 짧은 도입문
3. 관련 문서를 순서대로 보여주는 단계형 섹션 또는 Cards
4. 필요한 경우 권장 순서를 안내하는 Callout

예시:

```md
---
title: 파드(Pod) 생성
description: 할당된 GPU 자원으로 파드(Pod)를 생성하는 방법
---

## 1. Pods 메뉴 진입 및 생성 시작

① Pods 메뉴에 진입한 후 ② **Deploy**를 클릭해 파드 생성 화면으로 이동합니다.

![Pods 메뉴 진입 및 생성 시작](/docs/4-1-1.svg)
```

## 3. 제목 규칙

### 문서 제목

- frontmatter `title`이 문서 상단 제목으로 사용된다.
- 이 저장소의 문서 레이아웃은 frontmatter `title`과 `description`을 자동으로 표시하므로, 같은 제목의 H1을 본문에 반복하지 않는다.
- frontmatter `title`은 검색성과 용어 소개를 고려해 `파드(Pod) 생성`처럼 쓸 수 있다.

### 단계 제목

- 단계 제목은 문서형 명사구로 쓴다.
- 버튼 이름만 단독으로 쓰지 말고, 단계 목적이 드러나게 쓴다.
- 같은 문서 안에서는 제목 톤을 섞지 않는다.

좋은 예:
- `Pods 메뉴 진입 및 생성 시작`
- `파드 유형 선택`
- `약관 확인 및 동의`
- `조직 계정 생성 확인`

피해야 할 예:
- `메뉴 진입`
- `버튼 클릭`
- `생성 후 나오는 페이지`
- `표출 확인`

## 4. 본문 문장 규칙

- 본문은 사용자가 실제로 해야 할 행동을 순서대로 설명한다.
- 문장은 짧게 쓰고, 가능하면 한 문장 안에서 끝낸다.
- 번호 표시는 `① ② ③` 형식을 사용한다.
- UI 라벨은 굵게 표시한다.
- 설명용 일반 명사는 한국어로 쓴다.

좋은 예:
- `① Pods 메뉴에 진입한 후 ② **Deploy**를 클릭해 파드 생성 화면으로 이동합니다.`
- `① 조직 이름을 입력하고 ② 조직 계정 전환에 대한 안내를 확인한 후 동의 여부를 선택합니다.`
- `① GPU Pods 목록에서 생성한 파드가 표시되는지 확인합니다.`

피해야 할 예:
- `위 ②를 클릭합니다.`
- `버튼을 클릭합니다.`
- `생성 후 나오는 페이지를 확인합니다.`
- `표출되는지 확인합니다.`

## 5. UI 용어 표기 규칙

- 사용자가 화면에서 직접 찾아야 하는 메뉴, 버튼, 필드 이름은 UI 표기를 따른다.
- 한국어 설명과 UI 라벨이 함께 필요하면, 설명은 한국어로 쓰고 라벨만 원문을 유지한다.
- 기술 용어는 처음 한 번만 한영 병기하고, 이후에는 한국어 중심으로 정리한다.

예시:
- `파드(Pod)`은 첫 도입에서만 병기한다.
- 이후 본문은 `파드`로 쓴다.
- `Pods`, `Deploy`, `Connect`, `Launch Instance`, `Skip Filesystem`은 UI 라벨이므로 유지한다.

## 6. 이미지와 alt 텍스트 규칙

- 모든 단계 이미지는 해당 단계의 실제 화면과 직접 대응되어야 한다.
- alt 텍스트는 단계 제목과 같은 톤으로 맞춘다.
- alt 텍스트는 이미지 설명이지, 추가 해설이 아니다.

좋은 예:
- `![파드 유형 선택]`
- `![조직 계정 생성 확인]`
- `![VS Code 웹 페이지 확인]`

피해야 할 예:
- `![자원 선택]`처럼 실제 UI의 의미와 다른 alt 텍스트
- `![생성 확인]`처럼 확인 대상을 구체적으로 드러내지 않는 alt 텍스트

## 7. 확인 단계 규칙

- 확인 단계는 확인 위치와 확인 대상을 같이 적는다.
- 가능하면 목록명, 메뉴명, 역할명 등 화면 요소를 직접 적는다.

좋은 예:
- `① GPU Pods 목록에서 생성한 파드가 표시되는지 확인합니다.`
- `Organizations 목록에서 ① 생성한 조직과 역할이 표시되는지 확인하고, 우측 상단 프로필 메뉴에서 ② 조직 계정이 추가되었는지 확인합니다.`

피해야 할 예:
- `① 생성되었는지 확인합니다.`
- `페이지가 정상적으로 표시되는지 확인합니다.`처럼 위치와 대상을 쓰지 않은 문장

## 8. Callout 사용 규칙

- 사용자가 놓치기 쉬운 조건, 제한, 비용, 삭제 영향, 운영 정책만 Callout으로 뺀다.
- 본문과 같은 내용을 반복하지 않는다.
- Callout 종류는 의미에 맞게 선택한다.

권장 기준:
- `info`: 참고 정보, 현재 제한, 후속 절차
- `warning`: 삭제 영향, 데이터 손실 가능성, 주의 필요 사항

개요 페이지에서는 `info` Callout으로 권장 진행 순서나 참고할 흐름을 추가로 안내할 수 있다.

예시:
- `요금제를 선택해도 현재 즉시 과금되지는 않습니다.`
- `Skip Filesystem을 선택하면 별도 파일시스템을 사용하지 않습니다. 이 경우 파드를 삭제하면 파드에서 사용하던 파일도 함께 삭제됩니다.`
- `조직 생성만으로 GPU 자원이 자동 할당되지는 않습니다. 담당자 확인 후 GPU 자원이 할당됩니다.`

## 9. 법적·운영적 문구 규칙

- 약관, 동의, 요청, 권한 같은 문구는 회사에 불리하지 않게 쓴다.
- 사용자가 내용을 확인하고 판단해야 하는 단계는 그 의도가 문장에 드러나야 한다.
- 동의 문구는 동의를 강제하는 표현보다, 사용자가 내용을 확인한 뒤 동의 여부를 결정한다는 의미가 드러나게 쓴다.
- 자동 처리 여부는 사실대로 명확히 쓴다.

좋은 예:
- `조직 계정 전환에 대한 안내를 확인한 후 동의 여부를 선택합니다.`
- `팝업에서 약관 내용을 확인한 후 동의할 경우 ① **I agree to the above**를 클릭합니다.`
- `조직 생성이 완료되면 담당자에게 GPU 자원 할당을 요청합니다.`
- `조직 생성만으로 GPU 자원이 자동 할당되지는 않습니다.`

피해야 할 예:
- `약관 동의 버튼을 클릭합니다.`
- `안내를 확인한 후 동의합니다.`
- `연락 주시면 GPU 할당을 받으실 수 있습니다.`

## 10. 용어 통일 규칙

- 같은 대상은 같은 문서 안에서 같은 이름으로 쓴다.
- 상위 서비스 개념과 실제 리소스 개념이 다르면 구분해서 쓴다.
- 저장소 전체에서 공통으로 쓰는 용어 기준이 필요하면 `content/docs/glossary.mdx`를 우선 참고한다.

예시:
- 페이지 묶음 이름은 `컨테이너 생성`일 수 있다.
- 실제 생성 리소스는 `파드`로 통일한다.
- `컨테이너`, `인스턴스`, `파드`를 같은 문맥에서 섞어 쓰지 않는다.

## 11. 작성 전 체크리스트

- 제목이 문서형으로 통일되어 있는가
- description이 실제 범위만 설명하는가
- 본문이 실제 UI 흐름과 일치하는가
- 버튼과 메뉴 이름이 UI와 동일한가
- alt 텍스트가 단계 제목과 맞는가
- 확인 단계가 위치와 대상을 명확히 적었는가
- Callout이 중복 설명이 아닌가
- 법적 문구가 모호하지 않은가
- 같은 대상을 다른 이름으로 부르지 않았는가

## 12. 작성 순서 권장안

1. 이미지 기준으로 실제 UI 흐름을 먼저 확인한다.
2. frontmatter `title`과 `description`을 정한다.
3. frontmatter `title`과 단계 제목을 문서형으로 정리한다.
4. 본문은 UI 라벨 중심으로 짧게 쓴다.
5. 확인 단계와 Callout을 보강한다.
6. 마지막으로 alt 텍스트를 단계 제목과 맞춘다.

## 13. 적용 범위

이 규칙은 우선 아래 문서군에 적용한다.

- `content/docs/container-creation/*`
- `content/docs/organization-creation/*`
- `content/docs/account-creation.mdx`
- `content/docs/storage/*`
- `content/docs/network-settings/*`

새 가이드 문서를 추가할 때도 동일한 규칙을 기본값으로 사용한다.

## 14. 개요 페이지 규칙

- 개요 페이지는 절차를 상세히 설명하기보다, 사용자가 어떤 문서를 어떤 순서로 보면 되는지 빠르게 이해하게 해야 한다.
- 페이지 레이아웃이 frontmatter의 제목과 설명을 자동으로 표시하므로, 동일한 H1을 본문에 반복하지 않는다.
- 도입문은 환영 문구보다 문서 목적과 이용 대상을 먼저 설명한다.
- 단계형 섹션을 사용할 경우 제목만으로 순서가 드러나게 쓴다.
- Cards를 사용할 경우 제목은 실제 문서 제목과 맞춘다.
- 단계형 섹션이나 Cards의 설명은 각 문서에서 무엇을 할 수 있는지 짧게 정리한다.
